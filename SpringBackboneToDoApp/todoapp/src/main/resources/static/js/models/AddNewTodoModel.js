var app = app || {};

app.addNewToDo = Backbone.Model.extend({

    defaults:{
       
    },
    
    initialize : function(){
        console.log("addNewToDo model is created.");
    }

});